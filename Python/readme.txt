1. Please change the filename into mnist.tflite when you use this file
// 파일을 사용하실 때 파일명을 mnist.tflite로 변경해주세요

2. There are two mnist.tflite files. And I recommends you to use mnist.tflite(reference_code). Because mnist.tflite(my_code) doesn't work well, so it needs to be edited.
// 두 개의 mnist.tflite 파일이 있습니다. 그리고 mnist.tflite(reference_code)를 사용하길 권장합니다. 왜냐하면 mnist.tflite(my_code)는 제대로 작동하지 않기 때문이며, 수정이 필요합니다.

3. You can choose the Python file between .ipynb and .py 
